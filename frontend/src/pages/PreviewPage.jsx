// TODO: Mandatory preview step — user verifies extracted transactions before processing
// This step is non-negotiable per CLAUDE.md — never make it skippable
export default function PreviewPage() {
  return <div>PreviewPage</div>
}
